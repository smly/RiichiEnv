[package]
name = "riichienv-python"
version = "0.4.7"
edition = "2024"
license = "Apache-2.0"

[lib]
name = "_riichienv"
crate-type = ["cdylib"]

[dependencies]
riichienv-core = { path = "../riichienv-core", features = ["python"] }
pyo3 = { version = "0.28" }
serde_json = "1.0"
